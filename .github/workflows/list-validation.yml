name: List Validation

on:
  workflow_dispatch:

jobs:
  runs-on: list-validation
  build-cli:
    name: Building `wabbajack-cli.exe`
    
    steps:
      - name: Setup .NET Core SDK 6.0.x
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '6.0.x'
          include-prerelease: true
    


